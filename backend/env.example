# Server Configuration
PORT=3001
NODE_ENV=development

# Database Configuration (PostgreSQL)
# DB_HOST: Hostname or IP of PostgreSQL server
# DB_PORT: PostgreSQL server port (default: 5432)
# DB_NAME: Database name (create with: createdb rfp_management)
# DB_USER: PostgreSQL username
# DB_PASSWORD: PostgreSQL password
DB_HOST=localhost
DB_PORT=5432
DB_NAME=rfp_management
DB_USER=postgres
DB_PASSWORD=postgres

# Google Gemini API Configuration
# GEMINI_API_KEY: Get from https://makersuite.google.com/app/apikey
# Provides AI capabilities for RFP creation, response parsing, and proposal comparison
GEMINI_API_KEY=your_gemini_api_key_here

# Email Configuration (SMTP for sending RFPs to vendors)
# For Gmail: 
#   - Host: smtp.gmail.com, Port: 587 (TLS) or 465 (SSL)
#   - Generate App Password: https://myaccount.google.com/apppasswords
#   - Use App Password, NOT your regular Gmail password
# For other providers, update accordingly (Outlook, SendGrid, etc.)
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_USER=your_email@gmail.com
SMTP_PASSWORD=your_app_password_here
SMTP_FROM=your_email@gmail.com

# Email Configuration (IMAP for receiving vendor responses)
# For Gmail:
#   - Host: imap.gmail.com, Port: 993
#   - Use same App Password as SMTP
# IMAP polling checks for new emails every 5 minutes
IMAP_HOST=imap.gmail.com
IMAP_PORT=993
IMAP_USER=your_email@gmail.com
IMAP_PASSWORD=your_app_password_here

# Frontend URL for CORS configuration
FRONTEND_URL=http://localhost:3000
